'use client'

import React, { useState } from 'react'

const NavItems = () => {
  const [activeIndex, setActiveIndex] = useState<null | number>(null)
  return (
    <div className='flex gap-4 h-full'>
      {/* TODO: INSTER PRODUCTS */}
    </div>
  )
}

export default NavItems
